﻿@using Microsoft.AspNetCore.Http.Extensions
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"]</title>
    
    <!-- Open Graph tags for social media sharing -->
    <meta property="og:title" content="@ViewData["Title"]" />
    <meta property="og:type" content="website" />
    <meta property="og:url" content="@Context.Request.GetDisplayUrl()" />
    <meta property="og:image" content="@ViewData["Image"]" />
    <!-- Meta tags for SEO -->
    <meta name="description" content="@ViewData["Description"]" />
    <meta name="keywords" content="@ViewData["Keywords"]" />


    <meta name="robots" content="index, follow" />

    <!-- Lazy load CSS -->
    <link rel="preload" href="~/lib/bootstrap/dist/css/bootstrap.min.css" as="style" onload="this.onload=null;this.rel='stylesheet'">
    <noscript><link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css"></noscript>
    
    <link rel="preload" href="~/css/trangchu.css" as="style" onload="this.onload=null;this.rel='stylesheet'" asp-append-version="true">
    <noscript><link rel="stylesheet" href="~/css/trangchu.css" asp-append-version="true"></noscript>
    
    <link rel="preload" href="~/websitebenhvien.styles.css" as="style" onload="this.onload=null;this.rel='stylesheet'" asp-append-version="true">
    <noscript><link rel="stylesheet" href="~/websitebenhvien.styles.css" asp-append-version="true"></noscript>
    
    <link rel="preload" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" as="style" onload="this.onload=null;this.rel='stylesheet'">
    <noscript><link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css"></noscript>

    <!-- Defer non-critical scripts -->
    <script type="text/javascript" defer>
        function googleTranslateElementInit() {
            new google.translate.TranslateElement(
                { pageLanguage: 'vi', includedLanguages: 'vi,en,zh-CN,ja,ko'},
                'google_translate_element'
            );
        }
    </script>
    <script src="//translate.google.com/translate_a/element.js?cb=googleTranslateElementInit" async defer></script>
    <script type="application/ld+json">
        
        @Html.Raw(ViewData["Schema"])
        
    </script>
</head>
<body>

    @await Component.InvokeAsync("Headertrangchu")

<main role="main" style="padding-top: 250px;"  >
    @RenderBody()
</main>

<footer class="bg-white py-5 border-top fade-in w-100" >
    @await Component.InvokeAsync("Footertrangchu")
</footer>
    <!-- Nút cuộn lên đầu trang -->
<div class="zalo-chat-widget" 
                 id="zalo-chat-widget" 
                 data-oaid="3218616946017588505" 
                 data-welcome-message="Xin chào! Mình có thể giúp gì cho bạn?" 
                 data-autopopup="0" 
                 data-width="350" 
                 data-height="450"
                    style="justify-content: center !important; align-items: center !important; margin-bottom: 160px !important;">
    <div class="position-fixed bottom-0 end-0 m-4 d-flex flex-column gap-3 margin-right: 50px !important;" style="z-index: 1000;">
        <!-- Zalo Chat Widget -->
        <div class="d-flex flex-column align-items-end gap-3" style="margin-right: 82px!important;">
            <!-- Zalo Chat Widget -->


                
            </div>

            <!-- Chat box -->
            <div class="chat-box-wrapper">
                <!-- Chat Button -->
                <button id="chatButton" class="btn btn-primary rounded-circle shadow d-flex align-items-center justify-content-center position-relative animate__animated animate__pulse animate__infinite" 
                        style="width: 50px; height: 50px; background: linear-gradient(45deg, #0063EC, #00a5ff); border: none; transition: all 0.3s ease;"
                        data-bs-toggle="tooltip" 
                        data-bs-placement="left"
                        title="Tôi có thể giúp gì cho bạn?">
                    <i class="fas fa-comments fa-lg" style="color: white; filter: drop-shadow(0 0 2px rgba(255,255,255,0.5));"></i>
                    <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger" 
                          style="font-size: 0.6rem;" id="count-notification">
                        <i class="fas fa-bell text-white" id="count-notification"></i>
                    </span>
                    <div class="ripple position-absolute w-100 h-100 rounded-circle" style="animation: ripple 1.5s linear infinite; border: 2px solid #fff; opacity: 0;"></div>
                    <div class="bell-shake"></div>
                    <div class="position-absolute" style="bottom: 100%; right: 0; margin-bottom: 10px; background: rgba(0,0,0,0.8); color: white; padding: 8px 12px; border-radius: 4px; font-size: 14px; display: none;" id="chatTooltip">
                        Tôi có thể giúp gì cho bạn?
                    </div>
                </button>

                <!-- Chat Window -->
                <div id="chatWindow" class="card position-absolute bottom-100 mb-2 animate__animated animate__fadeIn" style="width: 300px; display: none; right: 0; transform-origin: bottom right;">
                    <div class="card-header bg-primary text-white d-flex justify-content-between align-items-center" style="border-radius: 10px 10px 0 0;">
                        <h6 class="m-0 new-label">
                            <i class="fas fa-circle text-success me-2" style="font-size: 10px;"></i>
                            Chat với chúng tôi
                        </h6>
                        <button type="button" class="btn-close btn-close-white animate__animated animate__rotateIn" id="closeChatBtn" style="transition: transform 0.3s;" onmouseover="this.style.transform='rotate(90deg)';" onmouseout="this.style.transform='rotate(0deg)';"></button>
                    </div>
                    <div class="card-body" style="height: 300px; overflow-y: auto; scroll-behavior: smooth;" id="chatMessages">
                        <!-- Welcome message -->
                        <div class="mb-2 animate__animated animate__fadeInLeft">
                            <div class="d-inline-block bg-light rounded p-2" style="position: relative;">
                                <p class="m-0">Xin chào! Chúng tôi có thể giúp gì cho bạn? 👋</p>
                                <div class="typing-indicator animate__animated animate__fadeIn">
                                    <span></span>
                                    <span></span>
                                    <span></span>
                                </div>
                            </div>
                        </div>
                    </div>
                    <div class="card-footer animate__animated animate__fadeInUp" style="border-radius: 0 0 10px 10px;">
                        <div class="input-group">
                            <input type="text" id="messageInput" class="form-control" placeholder="Nhập tin nhắn..." style="border-radius: 20px 0 0 20px;">
                            <button class="btn btn-primary animate__animated animate__pulse animate__infinite" id="sendMessageBtn" style="border-radius: 0 20px 20px 0; transition: all 0.3s;">
                                <i class="fas fa-paper-plane" style="transition: transform 0.3s;"></i>
                            </button>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Facebook Button -->
            <a href="https://www.facebook.com/benhvienmyphuoc?locale=vi_VN" target="_blank" 
               class="btn btn-primary rounded-circle shadow d-flex align-items-center justify-content-center animate__animated animate__bounceIn animate__delay-1s" 
               style="width: 50px; height: 50px; background: #1877f2; border: none;">
                <i class="fab fa-facebook-f fa-lg animate__animated animate__tada animate__delay-2s" style="color: white;"></i>
            </a>

            <!-- Back to top button -->
            <button id="backToTop" 
                    class="btn btn-primary rounded-circle shadow animate__animated animate__bounceIn animate__delay-2s" 
                    style="width: 50px; height: 50px;">
                <i class="fas fa-arrow-up animate__animated animate__bounce animate__infinite"></i>
            </button>
        </div>
    </div>
       

    <div id="google_translate_element" class="ms-2" style="display: none;" ></div>



</div>

<!-- Scripts -->
<script src="https://sp.zalo.me/plugins/sdk.js"></script>
<script src="~/lib/aspnet-signalr/signalr.js"></script>
<script src="~/lib/jquery/dist/jquery.min.js"></script>
<script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
<script src="~/js/site.js" asp-append-version="true" defer></script>
<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11/dist/sweetalert2.all.min.js"></script>

<!-- CSS -->
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/sweetalert2@11/dist/sweetalert2.min.css">

    <!-- Script xử lý nút cuộn lên đầu trang -->
    <script>
        
 $(document).ajaxError(function(event, jqXHR, ajaxSettings, thrownError) {
    if (jqXHR.status === 429) {
        // Tạo âm thanh cảnh báo
        const audio = new Audio('/Sound/alert.mp3');
        // Chỉ phát âm thanh nếu người dùng đã tương tác với trang
        if (document.documentElement.hasAttribute('data-user-interacted')) {
            audio.play().catch(function(error) {
                console.log("Không thể phát âm thanh:", error);
            });
        }

        // Ẩn các nút chat, facebook và back-to-top
        $('#chatButton').hide();
        $('.chat-box-wrapper').hide();
        $('a[href*="facebook"]').hide();
        $('#backToTop').hide();

        // Hiển thị thông báo
        Swal.fire({
            title: 'Cảnh báo!', 
            text: 'Bạn đã bị chặn',
            icon: 'error',
            showConfirmButton: false,
            allowOutsideClick: false,
            allowEscapeKey: false,
            imageUrl: 'https://cdn.shopify.com/s/files/1/0643/1735/8265/files/Meaning_Of_OMAE_WA_MOU_SHINDEIRU_480x480.jpg?v=1715871275',
            imageWidth: window.innerWidth,
            imageHeight: window.innerHeight,
            imageAlt: 'Loading...',
            grow: 'fullscreen'
        });
    }
});

// Đánh dấu khi người dùng tương tác với trang
document.addEventListener('click', function() {
    document.documentElement.setAttribute('data-user-interacted', '');
});
        // Xử lý hiệu ứng fade-in khi cuộn
        function checkFade() {
            const fadeElements = document.querySelectorAll('.fade-in');
            fadeElements.forEach(element => {
                const elementTop = element.getBoundingClientRect().top;
                const windowHeight = window.innerHeight;
                if (elementTop < windowHeight * 0.95) { // Tăng ngưỡng hiển thị
                    requestAnimationFrame(() => {
                        element.classList.add('visible');
                        element.style.transition = 'opacity 0.3s ease-out'; // Giảm thời gian transition
                    });
                }
            });
        }

        // Intersection Observer với threshold cao hơn để phát hiện sớm hơn
        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    requestAnimationFrame(checkFade);
                }
            });
        }, {
            threshold: 0.2 // Giảm ngưỡng để phát hiện sớm hơn
        });

        // Gọi hàm ngay khi DOM load xong
        document.addEventListener('DOMContentLoaded', () => {
            requestAnimationFrame(() => {
                checkFade();
                document.querySelectorAll('.fade-in').forEach(el => observer.observe(el));
            });
        });
    </script>

    @await RenderSectionAsync("Styles", required: false)
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>